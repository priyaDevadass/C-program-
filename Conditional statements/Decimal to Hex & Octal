#include <stdio.h>

int main() {
    int num;
    printf("Enter positive integer: ");
    scanf("%d", &num);

    if (num < 0) {
        printf("Invalid input\n");
        return 0;
    }

    printf("Hexadecimal: %X\n", num);
    printf("Octal: %o\n", num);

    return 0;
}
